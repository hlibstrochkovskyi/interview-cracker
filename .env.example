# Dev convenience only. In the shipped app, API keys live in the OS keychain
# (Electron safeStorage), never in a file and never in the renderer process.
ANTHROPIC_API_KEY=
DEEPGRAM_API_KEY=
CARTESIA_API_KEY=
